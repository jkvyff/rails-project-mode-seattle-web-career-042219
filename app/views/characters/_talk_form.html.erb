<%= form_for @character do |f| %>
  <%= f.collection_select :place_id, Character.all.where(place_id: @character.place_id), :id, :name, {:include_blank=>"Talk to"} %><br>
  <%= f.submit "Lets Talk" %>
<% end %>